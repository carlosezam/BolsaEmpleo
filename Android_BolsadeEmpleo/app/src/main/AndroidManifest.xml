<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ittap.bolsadeempleo">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ico_secretaria"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ico_secretaria"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PersonalInfoActivity" />
        <activity android:name=".cursos.ListaCursosActivity" />
        <activity android:name=".cursos.FormularioCursosActivity" />
        <activity android:name=".BaseListActivity" />
        <activity android:name=".curriculum.CompetenciaActivity" />
        <activity android:name=".curriculum.EscolarActivity" />
        <activity android:name=".curriculum.LenguajeActivity" />
        <activity android:name=".curriculum.TrabajoActivity" />
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity" />
        <activity android:name=".WebActivity" />
        <activity android:name=".SaveListActivity" />
        <activity android:name=".DetailsEmpleoActivity"></activity>
    </application>

</manifest>